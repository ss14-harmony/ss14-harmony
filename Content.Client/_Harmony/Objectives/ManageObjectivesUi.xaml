<ui:FancyWindow xmlns="https://spacestation14.io"
                xmlns:ui="clr-namespace:Content.Client.UserInterface.Controls"
                xmlns:gfx="clr-namespace:Robust.Client.Graphics;assembly=Robust.Client"
                VerticalExpand="True" HorizontalExpand="True"
                Title="Manage objectives"
                MinSize="400 200">

    <BoxContainer Orientation="Vertical">

        <BoxContainer>
            <Button Name="AddObjectiveAdminButton"
                Text="Add custom objective (Admin)"
                HorizontalExpand="True"
                Access="Public">
            </Button>
            <Button Name="AddObjectivePlayerButton"
                Text="Add custom objective (Player)"
                HorizontalExpand="True"
                Access="Public">
            </Button>
        </BoxContainer>

        <PanelContainer VerticalExpand="True" Margin="10 10 10 10">
            <PanelContainer.PanelOverride>
                <gfx:StyleBoxFlat BackgroundColor="#1B1B1E"/>
            </PanelContainer.PanelOverride>
            <ScrollContainer
                HScrollEnabled="False"
                HorizontalExpand="True"
                VerticalExpand="True">
                <BoxContainer
                    Name="ObjectivesBox"
                    Orientation="Vertical"
                    VerticalExpand="True"
                    Margin="10 10 10 0">
                </BoxContainer>
            </ScrollContainer>
        </PanelContainer>

    </BoxContainer>

</ui:FancyWindow>
